# Password Generator

## Description
A Python program that generates random passwords of specified length.

## How to Run
1. Open terminal
2. Navigate to the `Task3_Password_Generator` folder
3. Run: `python password_generator.py`

## Features
- Enter desired password length
- Minimum length enforced (8 characters)
- Uses uppercase, lowercase, numbers, and symbols
- Displays generated password
